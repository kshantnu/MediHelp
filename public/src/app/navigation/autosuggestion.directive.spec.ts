import { AutosuggestionDirective } from './autosuggestion.directive';

describe('AutosuggestionDirective', () => {
  it('should create an instance', () => {
    const directive = new AutosuggestionDirective();
    expect(directive).toBeTruthy();
  });
});
